node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

public/uploads

# Salida de compilacion
dist

# Respaldos locales de la base de datos (contienen datos de clientes)
backup-*.sql

# Marca del último respaldo y secreto TOTP del doble candado (ultra sensible)
backups/
